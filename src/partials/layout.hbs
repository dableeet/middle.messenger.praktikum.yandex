<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    {{#if globalStyleHref}}
    <link rel="stylesheet" href="{{ globalStyleHref }}" />
    {{else}}
    <link rel="stylesheet" href="../globals.scss" />
    {{/if}}
    {{#if styleHref}}
    <link rel="stylesheet" href="{{ styleHref }}" />
    {{/if}}
    <title>Practicum Chat</title>
  </head>
  <body>
    <div class="container">
      {{#if @partial-block}} {{> @partial-block}} {{/if}}
    </div>
  </body>
</html>
